#!/usr/bin/env bash
# bin/detect <build-dir>

BUILD_DIR=$1
if [ -f $BUILD_DIR/package.json ]; then
  echo "Node.js"
  exit 0
fi

echo "Did not detect package.json in build dir"
echo $BUILD_DIR
exit 1
